version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/one-bucket
    overwrite: true
permission:
  - object: /home/ubuntu/one-bucket/
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu
